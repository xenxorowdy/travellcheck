import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
    textCenter: {
        alignSelf:"center"
    },
    container: {
        flex: 1,
        padding: 12,
        gap:4
    }
})